pandas>=2.2.3
openpyxl>=3.1
scikit-learn>=1.4
joblib>=1.3
pdfplumber>=0.11
xgboost>=2.0        # optional — falls back to sklearn GradientBoostingRegressor if not installed
shap>=0.44          # optional — falls back to global feature_importances_ if not installed
anthropic>=0.40      # optional — only needed for live RAG generation; no-key fallback works without it
